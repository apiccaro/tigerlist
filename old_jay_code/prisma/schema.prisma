generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "file:./dev.db"
}

model User{
  id String = @id @default(cuid())
  email String
  preferredName String
}