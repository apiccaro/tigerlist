const nextConfig = {
    reactStrictMode: true,
    compiler: {
        styledComponents: true,
    },
};

module.exports = {
    reactStrictMode: true,
    images: {
      domains: ["images.app.goo.gl", "localhost", "www.science.org"],
    },
  }

exports = nextConfig;
